name: PR 520 SDK collections relay

on:
  push:
    branches:
      - refactor/durable-collections
    paths:
      - .github/workflows/pr520-sdk-collections-relay.yml
      - .phenix/pr520-sdk-collections.py

permissions:
  contents: write

jobs:
  apply-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/durable-collections
          fetch-depth: 0
      - name: Apply SDK collection implementation
        run: python .phenix/pr520-sdk-collections.py
      - name: Remove relay files
        run: |
          rm .phenix/pr520-sdk-collections.py .github/workflows/pr520-sdk-collections-relay.yml
          rmdir .phenix || true
      - name: Format
        working-directory: rust
        run: cargo fmt --all
      - name: Test persistence host semantics
        working-directory: rust
        run: cargo test -p phenix-core persistence
      - name: Test SDK durable collections
        working-directory: rust
        run: cargo test -p phenix-sdk durable::tests
      - name: Test SDK package
        working-directory: rust
        run: cargo test -p phenix-sdk
      - name: Push implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(sdk): add durable map and log collections"
          git push origin HEAD:refactor/durable-collections
